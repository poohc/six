<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
    
    <select id="selectIntroBoardCount" parameterType="java.util.HashMap" resultType="Integer">
		SELECT COUNT(*)		       
		  FROM ${boardName}
		 WHERE 1=1
		 <if test="title != null">
		   AND TITLE LIKE CONCAT('%', #{title} , '%')		   
		 </if> 
		 <if test="contents != null">
		   AND CONTENTS LIKE CONCAT('%', #{contents} , '%')
		 </if>
    </select>    
    
    <select id="selectIntroBoard" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT SEQ,
		       TITLE,
		       CONTENTS,
		       CREATE_USER_ID,
		       CREATE_DATE,
		       UPDATE_USER_ID,
		       UPDATE_DATE,
		       HIT_COUNT,
		       IS_NOTICE		       
		  FROM ${boardName}
		 WHERE 1=1
		   <if test="title != null">		       
		   AND TITLE LIKE CONCAT('%', #{title} , '%')
		   </if>
		   <if test="contents != null">
		   AND CONTENTS LIKE CONCAT('%', #{contents} , '%')
		   </if>
	  ORDER BY SEQ DESC
		   <if test="start != null">
		 LIMIT #{start},#{end}    
		   </if>
      		   
    </select>    
    
    <select id="getIntroBoardInfo" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        SELECT SEQ,
		       TITLE,
		       CONTENTS,
		       CREATE_USER_ID,
		       CREATE_DATE,
		       UPDATE_USER_ID,
		       UPDATE_DATE,
		       HIT_COUNT,
		       IS_NOTICE,
		       REF,
		       INDENT,
		       STEP		       
		  FROM ${boardName}
		 WHERE SEQ = #{seq}
    </select>
    
    <insert id="insertIntroBoard" parameterType="com.icon.six.vo.BoardVo">
    	INSERT INTO ${boardName}
			    	(
		    		   TITLE,
				       CONTENTS,
				       CREATE_USER_ID,
				       CREATE_DATE,
				       HIT_COUNT,
				       <if test="ref != null">
				           REF,
				       </if>
				       <if test="indent != null">
				           INDENT
				       </if>
				       <if test="step != null">
				           STEP
				       </if>
				       IS_NOTICE				       
			    	)
			 VALUES (
				 	   #{title},
				 	   #{contents},
				 	   #{createUserId},
				 	   now(),
				 	   0,
				 	   <if test="ref != null">
				           #{ref},
				       </if>
				       <if test="indent != null">
				           #{indent}
				       </if>
				       <if test="step != null">
				           #{step}
				       </if>
				 	   #{isNotice}		 		
			 		)      	
    </insert>
    
    <update id="updateIntroBoard" parameterType="com.icon.six.vo.BoardVo">
        UPDATE ${boardName}
         <set>
	          <if test="title != null">TITLE=#{title},</if>
	          <if test="contents != null">CONTENTS=#{contents},</if>
	      	  <if test="hitCount != null">HIT_COUNT=HIT_COUNT+1,</if>
	      	  <if test="isNotice != null">IS_NOTICE=#{isNotice},</if>
	      	  UPDATE_USER_ID = #{updateUserId},
		      UPDATE_DATE = now()
	    </set>
	    WHERE SEQ = #{seq}                   
    </update>
    
    <delete id="deleteIntroBoard" parameterType="java.util.HashMap">
        DELETE 
          FROM ${boardName}
         WHERE SEQ = #{seq}
    </delete>
    
</mapper>